## July 2nd
- What we want in a responsive ui
    - UI is dynamic
    - Responds to what the user does
    - Responds in relation to data
    - Trigger changes in data
    - Write behavior based on the data

- When stateful variable changes (fire x option)
    - State gives control over changing values
    - Automates re-rendering
    >Input element:         \
    >Output element:
    - Bind what should happen to the event
    - Not using state: we would have to get data off the monitor, do not reach accross the data ui divide -> 

- Onclick
- Onchange

- Good modularization
    - `onChange={onTodoTextChange}`
    - `value={todoItem}`

- Clean code
    - Code is self descriptive w.o reading

- Only one top level node in jsx?
- 

- Grids
    - 

- Arrays
    - Can't push to the array? can't add strongs and arrays
        - Spread operator -> taking all elements of a series (e.g) array without it actually being in that container.
        `const newTodos = [..alltoDos, todoItem]`
        - 

- Wire up inputs -> value={searchTitle}
- When use types -> OnChange happens
- 