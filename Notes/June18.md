## June 18th
- Node package manager
- Don't touch index.html
- index.js -> main entry point
- `<>` Fragment, every react component needs to return a single node


npx create-next-app components-example 
<- the last param is the name of the app you want to make; doesn't have to match mine

    - Settings:
      - **no** TypeScript
      - **no** linter
      - **yes/no** React Compiler
      - **no** tailwind
      - **no** src directory (just keeps things simple atm)
      - **no** AppRouter (for now)
      - **no** custom import alias
      - **no** agents
      - overall: no to everything, or yes to just React Compiler

dir

componenets into the root 
capital letters fer folder naming
jsx means its probs a component -> easy to discern what is what(good naming practice)
default ->not using default means you have multiple exports in one file
import component and call it in the jsx
make component>import it > use it
use props in jsx files
@absolute import paths -> default alias

